/*
 * Copyright (c) teris.io & Oleg Sklyar, 2017. All rights reserved
 */

plugins.apply(JavaPlugin)

dependencies {
	compileOnly(findbugsModule)
	compile(jacksonDataformatXmlModule)
	compile(jacksonDatatypeJSR310Module)
	compile(project(":rpc"))

	runtime(woodstockCoreAslModule)

	testCompileOnly(findbugsModule)
	testCompile(junitModule)
	testCompile(mockitoModule)
}
